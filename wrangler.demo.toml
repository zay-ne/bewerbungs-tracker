# Zweite, für sich stehende Auslieferung nur zum Vorzeigen:
# gleiche Oberfläche, aber ohne Konten und ohne Datenablage.
#   npx wrangler deploy -c wrangler.demo.toml
name = "bewerbungen-demo"
main = "sync/worker.js"
compatibility_date = "2026-01-01"

[assets]
directory = "./web"
binding = "ASSETS"

[vars]
DEMO_ONLY = "1"
